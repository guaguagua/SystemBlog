const assert=require('node:assert/strict'),path=require('node:path');const root=path.resolve(__dirname,'..');require(path.join(root,'bullets.js'));const B=globalThis.BlockBullets;assert.equal(B.names.length,6);for(let m=0;m<6;m++){const shots=B.emit(m,400,500,2);assert.ok(shots.length>=3);assert.ok(shots.every(b=>b.vy<0));B.advance(shots[0],.1);assert.ok(shots[0].y<457);}require(path.join(root,'item-vfx.js'));assert.equal(Object.keys(globalThis.BlockItemVFX.specs).length,50);
const {chromium}=require('C:/Users/wsy/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await browser.newPage({viewport:{width:1365,height:960}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.clock.install();await p.addInitScript(()=>{Math.random=()=>.999;window.Audio=class{play(){setTimeout(()=>this.onended?.(),300);return Promise.resolve()}pause(){}};});await p.goto('file:///'+root.replaceAll('\\','/')+'/index.html');
const result=await p.evaluate(()=>{const signatures=[];for(const entry of BLOCK_CURRICULUM.levels.flatMap(l=>l.words)){const c=document.createElement('canvas');c.width=500;c.height=400;const g=c.getContext('2d');const s=BlockSpells.create(entry,250,200,500,400,()=>.4);for(let t=0;t<10;t++)BlockSpells.update(s,.1,[{x:250,y:200}],()=>{});BlockSpells.ground(g,s,false);BlockSpells.draw(g,s,false);signatures.push(c.toDataURL());}return {count:signatures.length,unique:new Set(signatures).size};});assert.equal(result.count,50);assert.equal(result.unique,50);
await p.locator('#levelSelect button').nth(1).click();await p.locator('#start').click();
for(let i=0;i<12;i++){for(let k=0;k<35&&!await p.locator('#targetLabel').isVisible();k++)await p.clock.runFor(200);const t=await p.locator('#targetLabel').boundingBox(),a=await p.locator('canvas').boundingBox();await p.mouse.move(t.x+t.width/2,a.y+a.height*.81);for(let k=0;k<80&&await p.locator('#targetLabel').isVisible();k++)await p.clock.runFor(200);assert.equal(await p.locator('#targetLabel').isVisible(),false);if(i===7||i===11){await p.clock.runFor(800);await p.screenshot({path:path.join(root,`../../tmp/item-${i===7?'tree':'flower'}.png`),fullPage:true});await p.clock.runFor(1600);}else await p.clock.runFor(2400);}
assert.deepEqual(errors,[]);console.log('PASS: 50 distinct render signatures, 6 bullet patterns, actual tree/flower sentence effects, no browser errors.');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
