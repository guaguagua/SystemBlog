const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..');require(path.join(root,'spells.js'));require(path.join(root,'blizzard.js'));const S=globalThis.BlockSpells;
const data=JSON.parse(fs.readFileSync(path.join(root,'curriculum.json'),'utf8'));assert.equal(new Set(data.levels.flatMap(l=>l.words.map(e=>S.profile(e).name))).size,50);
for(const type of ['fire','blast','lightning','nature']){const s=S.create({id:type,spell:type},200,200,900,650,()=>.4);let hits=0;S.update(s,.1,[{x:200,y:200}],()=>hits++);assert.equal(hits,0);for(let i=0;i<45;i++)S.update(s,.1,[{x:200,y:200}],()=>hits++);assert.ok(hits>3);assert.equal(S.update(s,.1,[],()=>{}),false);}
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'C:/Users/wsy/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});try{const p=await browser.newPage({viewport:{width:1365,height:960}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.clock.install();await p.addInitScript(()=>{Math.random=()=>.999;window.Audio=class{play(){setTimeout(()=>this.onended?.(),400);return Promise.resolve()}pause(){}};});await p.goto('file:///'+root.replaceAll('\\','/')+'/index.html');await p.evaluate(()=>{window.casts=[];const a=BlockSpells.create,b=BlockBlizzard.create;BlockSpells.create=(...args)=>{casts.push(args[0].spell);return a(...args)};BlockBlizzard.create=(...args)=>{casts.push('ice');return b(...args)};});await p.locator('#start').click();
async function hit(){for(let k=0;k<30&&!await p.locator('#targetLabel').isVisible();k++)await p.clock.runFor(200);const t=await p.locator('#targetLabel').boundingBox(),a=await p.locator('canvas').boundingBox();assert.ok(t);await p.mouse.move(t.x+t.width/2,a.y+a.height*.81);for(let k=0;k<80&&await p.locator('#targetLabel').isVisible();k++)await p.clock.runFor(200);assert.equal(await p.locator('#targetLabel').isVisible(),false);}
for(let i=0;i<20;i++){await hit();assert.equal((await p.evaluate(()=>casts)).length,Math.floor((i+1)/4),`gate at hit ${i}`);if(i%4===3){await p.clock.runFor(900);await p.screenshot({path:path.join(root,`../../tmp/spell-${Math.floor(i/4)}.png`),fullPage:true});await p.clock.runFor(2100);assert.equal(await p.locator('#targetLabel').isVisible(),false,'no new target during ultimate');}else await p.clock.runFor(2400);}
await p.clock.runFor(1800);assert.ok((await p.locator('#overlayTitle').textContent()).includes('通关'));await p.locator('#start').click();for(let i=0;i<4;i++){await hit();await p.clock.runFor(i===3?900:2400);}assert.equal((await p.evaluate(()=>casts)).at(-1),'nature');await p.screenshot({path:path.join(root,'../../../tmp/spell-nature.png'),fullPage:true});await p.locator('#pause').click();const score=await p.locator('#score').textContent();await p.clock.runFor(2000);assert.equal(await p.locator('#score').textContent(),score);assert.deepEqual(errors,[]);console.log('PASS: all five spell families, 50 named profiles, delayed impact timelines, only English sentences cast, cooldown gating, pause.');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
